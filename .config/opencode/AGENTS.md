# Cileo Static Site OpenCode Config

This directory contains the static-site scoped OpenCode configuration for `cileo/`.

## Layout

- `rules/` — site-specific operational rules.

## Rules

| File | Purpose | Path |
|------|---------|------|
| `git-push-rule.md` | Prevent commit/push operations without explicit user authorization. | `rules/git-push-rule.md` |

## Notes

- No custom skills, workflows, system docs, or local OpenSpec copy are currently defined for the static site.
